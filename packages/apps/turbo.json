{
  "extends": ["//"],
  "tasks": {
    "build": {
      "env": ["NODE_ENV", "VITE_*"],
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/.env", "$TURBO_ROOT$/.env.*"],
      "outputs": ["dist/**"]
    }
  }
}
