; Conservative shared-host alignment. PHP-FPM / CGI read this from the
; document root (mod_php uses .htaccess php_value instead).
; Keep in sync with docker/php/uploads.ini.
post_max_size = 32M
upload_max_filesize = 32M
